services:
  - type: web
    name: categories-backend
    env: node
    rootDir: backend
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATA_DIR
        value: ../wash-tasnifoh/data
    autoDeploy: true

